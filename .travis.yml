sudo: false
language: python
python:
  - "nightly"
  - "3.6"
  - "3.5"
  - "3.4"
  - "2.7"
install:
  - pip install --upgrade setuptools pip wheel
  - pip install .
  - make install-test-deps
# command to run tests
script: make test
cache: pip
